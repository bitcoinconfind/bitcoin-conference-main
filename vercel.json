{
  "buildCommand": "npm ci --include=optional && npm run build",
  "installCommand": "rm -rf node_modules package-lock.json && npm install --legacy-peer-deps --include=optional",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
